
<style>
.lines::before {
  content: attr(alt);
  margin-right: 1em;
  color: gray;
  font-size: 80%;
}
.error_lines::before {
  content: attr(alt);
  margin-right: 1em;
  color: gray;
  font-size: 80%;
}

.error_lines {
  font-weight: bold;
}

.stderr_prefix::before {
  color: red;
  font-size: 80%;
  margin-right: 1em;
  content: '[stderr]';
}

</style>

<pre><% @data.each{|line| %>
<%= line %>
<% } %>
</pre>

<div><a href='/logfiles_/<%= @name %>'>raw logfile</a></div>
